app-id: com.sleepfiles.OSCAR
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: OSCAR20

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --talk-name=org.freedesktop.Notifications
  - --filesystem=home
  - --filesystem=/media
  - --filesystem=/run/media
  - --env=QT_QPA_PLATFORMTHEME=gtk3
  - --device=dri
build-options:
  env:
    - QMAKE_CXXFLAGS: -L/app/lib
    - QMAKE_LDFLAGS: -L/app/lib

modules:
  - shared-modules/glu/glu-9.json

  - name: OSCAR
    buildsystem: qmake
    config-opts:
      - OSCAR_QT.pro
      - LIBS+=-L/app/lib
    build-commands:
      - install -Dm755 oscar/OSCAR20 ${FLATPAK_DEST}/bin/OSCAR20
      - install -Dm644 Building/Linux/OSCAR20.svg ${FLATPAK_DEST}/share/icons/hicolor/scalable/apps/${FLATPAK_ID}.svg
      - desktop-file-edit --set-icon=com.sleepfiles.OSCAR Building/Linux/OSCAR20.desktop
      - desktop-file-edit --set-comment='Cross-platform sleep tracking program for CPAP and Oximetry data' Building/Linux/OSCAR20.desktop
      - install -Dm644 Building/Linux/OSCAR20.desktop ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dm644 com.sleepfiles.OSCAR.metainfo.xml -t ${FLATPAK_DEST}/share/metainfo/
      # Copy Translation and HTML files
      - cp -dpR oscar/{Translations,Html} $FLATPAK_DEST/bin/      
    sources:
      - type: git
        url: https://gitlab.com/CrimsonNape/OSCAR-SQL.git
        tag: v2.0.1
        commit: 377654fe6d80e8c408d0c7b050acbd0043f5a932
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)$
      - type: file
        path: com.sleepfiles.OSCAR.metainfo.xml
